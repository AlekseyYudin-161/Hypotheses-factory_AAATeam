[server]
# Отключаем файловый вотчер: он обходит __path__ всех модулей и падает на
# transformers → torchvision (лишние тяжёлые зависимости в venv). Побочный эффект —
# нет авто-перезагрузки при сохранении файла, перезапускай streamlit вручную.
fileWatcherType = "none"
